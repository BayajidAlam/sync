{
  "name": "video-processor",
  "version": "1.0.0",
  "description": "Video processing container for VisionSync platform",
  "main": "dist/process-video.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/process-video.js",
    "dev": "tsc && node dist/process-video.js",
    "clean": "rm -rf dist/"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.832.0",
    "@types/axios": "^0.9.36",
    "axios": "^1.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.1",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
